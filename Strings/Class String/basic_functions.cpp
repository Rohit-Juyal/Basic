#include <iostream>
#include <string>
using namespace std;

int main()
{
    // string str = "Hello";

    // str.resize(50);

    // cout << str.length() << endl;

    // cout << str.size() << endl;

    // cout << str.capacity() << endl;

    // cout << str.max_size() << endl;

    // str.clear();

    // cout << str;

    string str = "";

    if(str.empty())
    cout << "String is Empty";
    else
    cout << "String is " << str;

    return 0;
}